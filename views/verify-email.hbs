<!DOCTYPE html>
<html dir="ltr" lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đang xác nhận Email - EduTech</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Arial, sans-serif;
            background: #f5f5f5;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 20px;
        }
        .container {
            background: #fff;
            border: 1px solid #ddd;
            padding: 40px;
            max-width: 500px;
            width: 100%;
            text-align: center;
        }
        h1 {
            font-size: 24px;
            color: #333;
            margin-bottom: 20px;
        }
        p {
            color: #666;
            font-size: 14px;
            line-height: 1.6;
            margin-bottom: 15px;
        }
        .footer {
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid #eee;
            font-size: 12px;
            color: #999;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Đang xác nhận email...</h1>
        <p>Vui lòng chờ trong khi chúng tôi xác nhận email của bạn.</p>
        <div class="footer">
            <p>&copy; 2026 EduTech</p>
        </div>
    </div>
</body>
</html>
            height: 50px;
            border: 5px solid #333;
            border-top: 5px solid white;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin: 0 auto 20px;
        }
        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }
        h1 {
            margin-bottom: 10px;
            font-size: 24px;
        }
        p {
            margin-bottom: 10px;
            line-height: 1.6;
        }
        .status {
            margin-top: 30px;
            padding: 15px;
            background: #222;
            border-radius: 5px;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="spinner"></div>
        <h1>Verifying Your Email</h1>
        <p>Please wait while we verify your email address...</p>
    </div>

    <script>
        // Submit verification request
        async function verifyEmail() {
            try {
                const response = await fetch('{{apiUrl}}/api/v1/auth/verify-email', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json',
                    },
                    body: JSON.stringify({
                        token: '{{token}}'
                    })
                });

                if (response.ok) {
                    // Redirect to success page
                    window.location.href = '{{apiUrl}}/api/v1/auth/email-verified';
                } else {
                    const error = await response.json();
                    // Redirect to error page
                    window.location.href = '{{apiUrl}}/api/v1/auth/verification-error?reason=' + encodeURIComponent(error.message || 'Verification failed');
                }
            } catch (error) {
                console.error('Verification error:', error);
                window.location.href = '{{apiUrl}}/api/v1/auth/verification-error?reason=' + encodeURIComponent('Network error');
            }
        }

        // Auto-verify on page load
        window.addEventListener('load', verifyEmail);
    </script>
</body>
</html>